{
  "name": "check-frontenda-user",
  "private": true,
  "workspaces": ["apps/user-app"],
  "scripts": {
    "dev": "npm run dev --workspace=user-app",
    "build": "npm run build --workspace=user-app",
    "start": "npm run prod --workspace=user-app",
    "start:server": "npm run start --workspace=user-app",
    "lint": "npm run lint --workspace=user-app"
  }
}
